import { useScheduler } from '#scheduler';

// export default defineNitroPlugin(() => {
//   removeDuplicates();
// });

// function removeDuplicates() {
//   const scheduler = useScheduler();

//   scheduler
//     .run(() => {
//       console.log('Removing duplicate distances, steps, and flights');
//     })
//     .dailyAt(0, 0);
// }
